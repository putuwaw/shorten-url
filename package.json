{
  "name": "shorten-url",
  "description": "URL Shortener using Flask and MySQL",
  "license": "MIT",
  "devDependencies": {
    "tailwindcss": "^3.4.3"
  },
  "scripts":{
    "test": "tailwindcss -i ./static/styles/input.css -o ./static/styles/output.css --minify"
  }
}
